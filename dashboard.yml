---
- hosts: new-dashboard-dev
  remote_user: root
  roles:
    - common/python2
    - dashboard/registry_refresher
    - dashboard/stats
    - dashboard/dashboard

  vars:
    custom_user: dashboard
