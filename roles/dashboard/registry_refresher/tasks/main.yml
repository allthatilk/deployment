---
- include_role:
  name: common/install_default_packages

include_role:
  name: common/clone_repo

- name: Create initial empty directories for RR
    file:
      path: "{{repo_dir}}{{ item }}"
      state: directory
    with_items:
      - urls
      - data
      - ckan
